name: Build Geode Mod

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build Mod
    uses: geode-sdk/build-geode-mod/.github/workflows/build.yml@v3
    with:
      target: 'Android64'
      
